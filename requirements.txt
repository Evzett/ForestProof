# Зависимости расчётного слоя: tools/ и forestproof_core/.
# Зависимости веб-сервиса — отдельно, в api/requirements.txt.
#
# Прогон расчёта по набору кейса:
#     pip install -r requirements.txt
#     python tools/extract_case_data.py --data data --out app/src/data/case-data.json --maps app/public/maps

numpy==2.2.1

# Чтение растров Sentinel-2 в tools/sentinel_evidence.py.
# imagecodecs обязателен: файлы отражения записаны с предиктором 3
# (floating point), и без него tifffile падает на первом же снимке.
# Собственный читатель tools/geotiff.py обходится без этих двух пакетов —
# он написан как раз чтобы не тащить геобиблиотеки в основной расчёт.
tifffile==2025.1.10
imagecodecs==2024.12.30

# Рендер превью, слоёв карты и изображений событий.
pillow==11.0.0
