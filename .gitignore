node_modules/
__pycache__/
dist/
dist-ssr/
*.local
.env
.env.*
!.env.example

# логи
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log

# редакторы и ОС
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Кэш исходных растров: сотни мегабайт, в репозиторий не кладём
data/cache/

# Растры набора кейса: 44 МБ, раздаются отдельным архивом «Зеленые инвестиции DATA».
# Описания набора (areas, events, scenes, sources, file_catalog, methodology)
# остаются в репозитории: они маленькие, и без них расчёт не воспроизвести.
data/**/*.tif
data/scene_metadata.json

# Локальные дополнения к набору не добавляем; уже отслеживаемые CSV остаются.
/data/

# Локальное виртуальное окружение Python (README создаёт его в .venv).
.venv/

# Кэш скачанных продуктов и картинки расчётов по контурам пользователя.
# Всё это выводится из открытых источников по запросу и весит гигабайты.
api/data/cache/
api/data/contours/
