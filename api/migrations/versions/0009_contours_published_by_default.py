"""Контур виден по умолчанию

Revision ID: 0009
Revises: 0008
Create Date: 2026-09-19

Обратное решение к 0006, принятое по результату проверки.

Там контуры создавались скрытыми: «человек загружает файл, чтобы
посмотреть, а не чтобы сразу показать всем». На практике это дало
поведение, неотличимое от потери данных: загрузил участок, подождал
несколько минут расчёта, вышел из учётной записи — и участок исчез из
каталога и из сводки на первом экране. Данные были на месте, увидеть их
было нельзя.

ForestProof — скрининговый инструмент для команды. Загруженный контур
здесь рабочий результат, который коллеги должны видеть. Спрятать его
автор может переключателем в профиле: теперь осознанное действие
требуется для сокрытия, а не для показа.

Уже загруженные контуры раскрываются. В 0006 написано обратное — что
раскрывать чужое задним числом нельзя, — и там это было верно: правило
вводилось для многопользовательской работы. Сейчас в базе лежат только
контуры демонстрационной учётной записи администратора, заведённые до
того, как публикация начала что-либо значить. Скрывать их не от кого.
"""
from alembic import op
import sqlalchemy as sa

revision = "0009"
down_revision = "0008"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.alter_column("saved_contours", "published", server_default=sa.text("true"))
    op.execute("UPDATE saved_contours SET published = true")


def downgrade() -> None:
    op.alter_column("saved_contours", "published", server_default=sa.text("false"))
