# Локально docker-compose подставляет DATABASE_URL сам (см. docker-compose.yml).
# Этот файл — для запуска api вне контейнера (venv) или для справки.
DATABASE_URL=postgresql+psycopg2://forestproof:forestproof@localhost:5432/forestproof
ENVIRONMENT=development
