Вот текст, можно кидать как есть.

---

**По `projects.json` — структура идеальная, вопросов ноль. Но по содержимому набралось на список. Сначала три вопроса, они блокирующие.**

**В1. Откуда полигоны?** В открытой выгрузке реестра границ проектов нет. Если `proj-01.geojson` обведён на глаз по лесничеству — это нормально, но тогда надо это подписать на экране, иначе получится, что мы считаем не по проекту, а по своему представлению о нём. Если границы официальные — скинь ссылку на источник, она пойдёт в блок происхождения.

**В2. GEE тянет 505 тысяч га?** Hansen на 30 м — это 5,6 млн пикселей на полигон, Dynamic World на 10 м — около 50 млн, и так по каждому году из двенадцати. Проверь сводку по proj-01 прямо сейчас, до старта. Если валится по таймауту, лучше знать сегодня.

**В3. `project_id` в `projects.json` совпадают с теми, что у тебя в `data/geo/`?** Если разъедутся, расчётное ядро соберёт пустоту, и мы это поймаем только на первом чекпоинте.

---

**П1. Заменить proj-02 на реальный проект.**
«КП-000045-МОД» и «Сибирский углерод» выдуманы, и это видно: формат номера не совпадает с реальным `04-2023-00000012` рядом, период ровно с 1 января по 31 декабря. `source: manual` мы честно показываем на экране, но если жюри откроет реестр и не найдёт компанию — просядет доверие ко всей демонстрации, а не к одной строке.

В выгрузке 132 проекта. Нужен реальный, желательно **лесовосстановление или лесоразведение, 20–50 тысяч га**. Два условия: методология, где эффект — это прирост, а не предотвращённые выбросы (объясню в П2), и масштаб, сопоставимый с остальными (П4).

**П2. У proj-01 строку с эффектом пометить `comparable: false`.**
Методология — охрана лесов от пожаров. Заявленные 361 600 т/год — это **предотвращённые** выбросы: сколько сгорело бы без авиамониторинга. Величина контрфактическая, со спутника не наблюдается. Мы базовые линии не считаем и не претендуем.

Поэтому сравнивать её с нашим наблюдаемым изменением запаса нельзя — получится красивое, но бессмысленное расхождение. Ставим `comparable: false`, статус по проекту собирается из остальных строк.

Это не потеря, а плюс: у нас в требованиях есть статус «показатели напрямую не сопоставимы», и до сих пор он был чисто теоретическим. Теперь под него есть живой случай, и на защите это звучит лучше, чем если бы всё сошлось.

**П3. Разобраться, что такое `forest_area_ha: 504986`.**
Это площадь территории проекта или лесопокрытая площадь? Почти наверняка первое — границы лесничества целиком. А мы по Hansen считаем именно лесопокрытую, и в Красноярском крае внутри лесничества лес это 75–85%: остальное болота, старые гари, реки, гольцы.

Если сравнить в лоб, получим расхождение процентов в двадцать, которое целиком является артефактом определения. И напишем «требует проверки» про реальную компанию на пустом месте.

Нужно от тебя: посмотри в выгрузке, как называется эта графа, и добавь в `claims` поле `area_kind` со значением `project_territory` или `forest_cover`. Дальше уже дело расчётного ядра — сравнивать с тем же самым.

**П4. По масштабу.**
504 986 / 25 000 / 31 500 — разница в двадцать раз. Карта, чтобы вместить первый, отзумится так, что два других станут точками, а 18 га свежих нарушений на 25 тысячах и на полумиллионе в одной колонке читаются одинаково, хотя это разные новости. Если получится подобрать второй реальный проект тысяч на 30–60 — половина проблемы уйдёт сама, остальное доберём на фронте.

**П5. Даты.**
У proj-01 `monitoring_date: 2023-11-28`, а спутник мы считаем на сентябрь 2026. Если сверять заявленное в 2023 с наблюдаемым в 2026, расхождение вылезет у всех проектов просто потому, что прошло три года.

Это мой недосмотр в контракте, я его сегодня допишу: сверка идёт **на дату мониторинга**, наблюдение берётся ближайшее валидное к ней, а не последнее доступное. От тебя ничего не нужно, просто имей в виду — при выборе второго проекта лучше тот, у кого мониторинг посвежее.

---

**Итого по приоритету:** ответь на три вопроса, замени proj-02, добавь `area_kind`. Остальное на мне и на расчётном ядре. Структуру файла не трогай, она правильная.

---

Параллельно допишу в контракт пункт про дату сверки и про базу площади — их там действительно не хватает, и без них Р3 сделает сверку неправильно даже при идеальных входных данных. Сказать, когда будет готово?