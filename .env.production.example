# Copy to .env before starting docker-compose.production.yml.
# Generate secrets locally, for example: python -c "import secrets; print(secrets.token_urlsafe(48))"
POSTGRES_PASSWORD=
AUTH_SECRET=
FORESTPROOF_ANALYST_PASSWORD=
FORESTPROOF_ADMIN_PASSWORD=
HTTP_PORT=80
CORS_ORIGINS=http://localhost
ROUTERAI_API_KEY=
